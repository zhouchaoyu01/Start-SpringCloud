spring:
  application:
    name: userservice # 服务名称
  profiles:
    active: dev #开发环境，这里是dev
  cloud:
    nacos:
      server-addr: 192.168.10.199:88 # Nacos集群地址
      config:
        file-extension: yaml # 文件后缀名